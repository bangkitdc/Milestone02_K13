import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  footer: {
    position: "relative",
    bottom: 0,
    left: 0,
    color: "white",
    margin: "30px 0",
    textAlign: "center",
    width: "100%",
  },
}));
